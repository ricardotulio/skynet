version: '3'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.development
    volumes:
      - ./:/app
    env_file: .env

  nginx:
    image: nginx
    volumes:
      - ./infra/nginx/etc/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 8000:80
